# 独立开发者产品数据库 MVP 实施计划

## 目标

将 `1c7/chinese-independent-developer` 的 Markdown 数据转为结构化产品数据库，并提供可搜索、可筛选、可视化的静态网站。

## MVP 范围

1. 数据同步与四类源文件解析
2. 标准化 JSON 与数据质量报告
3. 产品全文搜索及类型、年份、城市、状态筛选
4. 年度趋势、类别占比、城市分布、状态分布
5. 测试、数据校验、静态构建和 GitHub Actions
6. 保留源文件、章节、行号和原始记录以便追溯

## 验收标准

- `npm run test` 通过
- `npm run check:data` 通过
- `npm run build` 生成完整 `dist/`
- 上游同步失败时，本地开发可回退到固定 fixtures
- CI 环境同步失败时直接失败，防止误发布示例数据
- 页面可完成全文搜索与组合筛选
- 所有可视化随筛选条件联动
- 数据代表性和授权边界有明确说明

## 后续阶段

- 产品详情页和开发者详情页
- 链接健康检查及状态变更历史
- 城市地图和开发者—产品关系图谱
- 周报/月报增量生成
- 数据 API、CSV/Parquet 导出
- 人工审核后台与修正规则
