{
  "name": "chinese-indie-developer-database",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "scripts": {
    "sync:data": "node scripts/sync-data.mjs",
    "sync:taxonomy": "node scripts/sync-taxonomy.mjs",
    "sync:github": "node scripts/sync-github.mjs",
    "build": "node scripts/build.mjs",
    "check:data": "node scripts/check-data.mjs",
    "check:taxonomy": "node scripts/check-taxonomy.mjs",
    "check:js": "node --check src/app.js && node --check src/separated-insights.js && node --check src/taxonomy-insights.js",
    "test": "node --test tests/*.test.mjs",
    "verify": "npm run test && npm run check:js && npm run sync:taxonomy && npm run check:data && npm run check:taxonomy && npm run build"
  },
  "engines": {
    "node": ">=20"
  }
}
